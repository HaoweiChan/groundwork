# Done — one line per merged task

Append-only. Details live in the task's ADRs/PR and `tasks/pr-loop-ledger.jsonl`;
full blocks live in git history. Format: `- <id> — <title> (<merge date>) — <refs>`
