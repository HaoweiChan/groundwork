# Tasks — pr-loop queue

Format and rules: the installed groundwork plugin's `pr-loop` skill. Two
sections only — merged work moves to `tasks/DONE.md` as one-liners. Ask the host
to run the `ready.py` script resolved beside that skill to list unblocked tasks.

## Queue

## Debt
